export const db_host = String(process.env.PGHOST)
export const db_port = Number(process.env.PGPORT)
export const db_name = String(process.env.PGNAME)
export const db_user = String(process.env.PGUSER)
export const db_password = String(process.env.PGPASSWORD)

export const postersPath = "/home/node/app/posters"
